# JavaScript-sumAll-function
To run the function, console.log(sumAll())
with two arguments containing integers.
The function will sum the total numbers between the two arguments.
Or run the test file.
